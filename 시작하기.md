# NDT Assistant — 시작하기

비파괴검사 기성청구 검증 도구 + AI 개발환경. **명령어를 한 줄도 입력하지 않습니다.**
모든 작업은 `.bat` 파일 더블클릭으로 합니다.

---

## 1. 설치 — 순서대로 3번 더블클릭

| 순서 | 파일 | 하는 일 | 걸리는 시간 |
|---|---|---|---|
| ① | **`install.bat`** | Python + 필요한 라이브러리 설치 | 1~3분 |
| ② | **`setup_vscode.bat`** | VS Code + AI 도우미 설치 *(선택)* | 3~5분 |
| ③ | **`install_extras.bat`** | 그래프·Word·엑셀 서식 라이브러리 *(선택)* | 1분 |

- **①만 해도 프로그램은 완전히 동작합니다.** ②③은 있으면 편한 것들입니다.
- 관리자 권한이 필요 없고, 인터넷도 필요 없습니다. 전부 이 폴더 안에 들어 있습니다.
- **여유 공간 약 3GB**가 필요합니다. (② VS Code 까지 설치하면 4GB 여유 권장) 한글·공백 없는 경로(예: `D:\NDT_Assistant\`)를 권장합니다.

### ⚠ ② 를 실행하기 **전에** API Key 를 먼저 등록하세요

`setup_vscode.bat` 은 `NDT_STUDIO_TOKEN` 이 없으면 AI 연결 설정을 **비어 있는 채로**
만들어 버립니다. 그러면 `Ctrl+L` 을 눌러도 인증 실패만 봅니다.
키를 먼저 등록하고 **로그아웃·재로그인** 한 뒤 ② 를 실행하세요.

Windows 검색 → "환경 변수" → 새로 만들기:

| 변수 이름 | 값 | 용도 |
|---|---|---|
| `NDT_HCX_TOKEN` | HCX API Key (`eyJpdiI6…`) | 프로그램의 AI 판정 |
| `NDT_STUDIO_TOKEN` | Studio XP API Key (`lsak-…`) | Gemma·gpt-oss, VS Code AI |

등록 후 **로그아웃·재로그인** 해야 적용됩니다.

> **값에는 키만 넣으세요.** 2026-09-02 실제로 여기서 막혔습니다.
> 프로그램이 `Bearer ` 를 자동으로 붙이기 때문에, 값에 또 넣으면
> `Bearer Bearer eyJ...` 가 되어 인증이 실패합니다 (`code=40102`).
>
> | 잘못된 값 | 문제 |
> |---|---|
> | `Bearer eyJpdiI6...` | `Bearer` 가 두 번 |
> | `"eyJpdiI6..."` | 따옴표가 키에 섞임 |
> | `eyJpdiI6... ` | 뒤에 공백·줄바꿈 |
>
> 확인: PowerShell 에서 `$env:NDT_HCX_TOKEN.Substring(0,8)` → `eyJpdiI6` 이면 정상.

> API Key는 이 압축파일에 들어있지 않습니다. 발급 메일에서 직접 가져와 등록하세요.

### 네트워크 설정 2가지

| 항목 | 내용 | 누가 |
|---|---|---|
| 방화벽 아웃바운드 | HCX `10.0.0.10:8443` / Studio XP `10.0.0.11:8443` — **이 2개면 충분** | **IT 요청 필수** |
| hosts 등록 | `C:\Windows\System32\drivers\etc\hosts` 에 `10.0.0.11 studio.example.internal` 한 줄 | 관리자 권한 있으면 직접 |

**`사내IT_신청양식.md` 를 열어 복사해 보내면 됩니다.** hosts 를 직접 등록했다면 방화벽만 요청하면 됩니다.

> hosts 파일이 있는 폴더는 **일반 사용자도 들어가지고 열어볼 수 있습니다.** 막히는 건 저장할 때입니다.
> 메모장을 **관리자 권한으로 실행**해서 열면 저장이 되고, 다른 계정 비밀번호를 물어보면 권한이 없는 것입니다.
> 자세한 확인 방법은 `사내IT_신청양식.md` 의 "hosts, 내가 직접 할 수 있나" 참고.
> Studio XP는 내부 DNS에 없어서 이 등록을 안 하면 주소를 못 찾습니다.

---

## 2. 프로그램 — `NDT_Assistant.bat` 더블클릭

GUI 창이 열립니다. 검은 창은 진행 상황 표시용이며 끝나면 닫힙니다.

### 매달 하는 일

**① 청구회차 검토 시작** — 이 도구의 핵심 기능입니다.

시공사이 보낸 **청구 엑셀**과 그 회차의 **성적서 PDF**를 넣으면, 청구 한 줄 한 줄을 이렇게 검증합니다:

- **성적서 매칭** — 청구한 검사가 실제 성적서에 있는지 대조합니다. 규칙으로 먼저 맞춰보고, 애매한 것만 AI에게 묻습니다.
- **도면 대조** — 그 조인트에 도면이 요구하는 검사 종류(VT/PT/UT/RT)와 청구 내역이 맞는지 봅니다.
- **과다·누락 판정** — 도면에 없는 검사를 청구했는지(과다), 해야 할 검사를 빠뜨렸는지(누락) 가려냅니다.
- **기준 적합성** — 인용된 규격 조항이 그 품목에 실제로 적용되는 조항인지 확인합니다. (철근인데 강구조물 규격을 인용한 경우 등)
- **위험도·재확인 표시** — 확실한 위반은 빨강, 도구가 자신 없는 건은 파랑으로 "재확인 필요"를 답니다. **파랑을 빨강보다 먼저 보세요.** 사람 판단이 필요하다는 뜻입니다.

결과는 42개 열짜리 엑셀로 나옵니다: 매칭된 성적서 번호·검사방법·점수, 도면 요구 NDT, 과다/누락, 적합성 판정, 근거 문서와 조항 페이지, 권장 조치까지 들어 있습니다.

### 자료가 들어올 때만

**② 신규 도면 등록** / **③ 표준·SCWEP 등록** — 도면이나 규격 PDF를 넣고 한 번 누르면 도구가 내용을 읽어 **영구 기억**합니다. 이후 매달 검토할 때 자동으로 참조합니다. 재등록은 필요 없습니다.

### 보고할 때

**④ 검사기준 가이드 생성** — 품목별로 어떤 검사가 왜 요구되는지를 규격 조항과 함께 정리한 HTML을 만듭니다. 브라우저에서 열리고, 외부 인터넷 연결이 전혀 없어 폐쇄망에서도 그대로 열립니다. 시공사·검사기관과 협의할 때 그대로 인용할 수 있습니다.

**⑤ 검토 대시보드** — 여러 명이 같이 볼 때, 또는 행 단위로 정정 내용을 누적 입력할 때 씁니다.

### 관리·문제 대응

**⑥ HCX 연결 점검** — 설치 후 처음에 한 번 누르세요. 토큰·방화벽·실제 호출을 차례로 진단해 어디가 막혔는지 알려줍니다.

**⑦ 업데이트 적용** — 사외에서 받은 패치를 적용합니다. 미리보기와 자동 백업이 붙어 있습니다.

**⑧ 문제 발생 보고** — 로그를 한 번에 모아 zip으로 만듭니다. **도면·청구 원본은 빠지므로 사외 반출이 가능합니다.** 이걸 개발자에게 보내면 됩니다.

---

## 3. AI 개발환경 — `run_vscode.bat` 더블클릭

`setup_vscode.bat`을 실행했다면 쓸 수 있습니다. VS Code가 이 프로젝트 폴더를 연 채로 뜹니다.

**`Ctrl+L`** 을 누르면 오른쪽에 AI 대화창이 열립니다. 사내 모델 세 개가 이미 연결되어 있습니다:

| 모델 | 성격 | 언제 쓰나 |
|---|---|---|
| **Gemma 4 31B** | 주력. 컨텍스트 256K | 코드 설명, 수정, 일반 질문. 규격 문서를 통째로 붙여넣고 물어볼 수 있음 |
| **gpt-oss-120b** | 추론 특화 | 원인을 모르겠는 오류, 논리 검토 |
| **HCX Seed 32B** | 한국어 강함 | 보고 문장 다듬기, 한국어 설명 |

대화창 위쪽에서 모델을 바꿀 수 있습니다.

### 이렇게 쓰면 좋습니다

- 코드를 드래그하고 `Ctrl+L` → "이 함수가 무슨 일을 하는지 설명해줘"
- 에러 메시지를 붙여넣고 → "이게 무슨 뜻이고 어디를 고쳐야 해?"
- `config\hcx.yaml` 을 열어놓고 → "A/B 비교를 켜려면 어디를 고쳐야 해?"
- 규격 원문을 붙여넣고 → "이 조항이 매립철물에 적용되는지 판단해줘"

### 기대치를 정확히 알아두세요

사내 모델은 **코드 설명, 작은 수정, 에러 해석, 규격 문장 검토**에는 충분합니다.
반면 "이 기능 전체를 만들어줘" 같은 큰 작업은 어렵습니다. 그런 건 사외 개발자에게 요청하세요.

### 미리 설정해 둔 것

- **사용 기록 전송 끔** — 폐쇄망에서 외부로 나가려다 멈추는 것을 막습니다.
- **자동 업데이트 끔** — 인터넷을 찾지 않습니다.
- **`.continueignore`** — `samples\`(도면·성적서), `data\`(추출 결과) 폴더는 AI에게 보내지 않습니다.
  (반입 심사가 점으로 시작하는 파일을 막아서 `continueignore.txt` 로 들어오고,
  `setup_vscode.bat` 이 원래 이름으로 되돌려 놓습니다. 따로 하실 일 없습니다.) 사내 AI라 망 밖으로 나가진 않지만 매번 딸려 보낼 이유가 없습니다.
- **YAML 들여쓰기 보호** — `hcx.yaml` 같은 파일에서 한 칸만 틀려도 파일이 깨지는데, 화면에 즉시 표시됩니다.
- **프로젝트 맥락 주입** — "폐쇄망이라 새 패키지 설치는 불가", "잘못된 판정은 금액 오류로 이어지니 확실하지 않으면 추측하지 말 것" 같은 지침을 AI에게 미리 걸어두었습니다.

---

## 3-1. 규격 문서 검색 — AI 판정의 근거를 넣어주는 부분

AI 가 "근거 없음"이라 답하거나 엉뚱한 답을 하는 가장 큰 이유는 **규격 원문을
못 찾아서**입니다. 규격 원문(GOST, SP 70 등)을 적재해 두면 판정할 때 관련
조항을 찾아 함께 넣어 줍니다.

### 1) 규격 PDF 적재 (최초 1회, 이후 문서 추가 시)

규격 PDF 를 한 폴더에 모아 두고:

```
run_ingest_standards.bat <폴더경로> code
```

예) `run_ingest_standards.bat D:\규격원문 code`

한국어·영어·러시아어 원문 모두 됩니다. 시간은 문서당 수 초~수십 초입니다.

### 2) 임베딩 — 한국어 질문으로 영어·러시아어 원문을 찾게 해 주는 것

규격 원문은 영어·러시아어인데 질문은 한국어라, 단어 맞추기로는 못 찾습니다.
사내 AI(Studio XP)의 **bge-m3** 가 언어를 넘어 뜻으로 찾아 줍니다.

- `NDT_STUDIO_TOKEN` 이 등록돼 있으면 1) 적재 때 **자동으로** 함께 계산됩니다.
- 이미 적재한 뒤에 토큰을 등록했다면 명령창에서 한 번:

```
call set_env.bat
%PYTHON% -m app.main reindex-standards
```

- 임베딩이 안 되는 상황(키 없음·서버 점검)에도 검색은 **단어 맞추기로 계속 동작**합니다.
  0건으로 떨어지지 않게 만들어 두었습니다.

### 3) 검색이 잘 되는지 눈으로 확인

```
call set_env.bat
%PYTHON% -m app.main search-standards "철근 용접부 VT 는 전수검사인가 배치 표본검사인가"
```

맨 윗줄에 `검색 방식: hybrid` 가 나오면 임베딩까지 쓰고 있는 것이고,
`bm25` 면 단어 맞추기만 쓰는 상태입니다. 아래에 조항 5개가 문서명·쪽수와 함께 나옵니다.

### 사내에서 임베딩이 열려 있는지 확인 (PowerShell, 1회)

```
curl.exe -k -i -m 30 https://studio.example.internal:8443/v1/embeddings -H "Authorization: Bearer $env:NDT_STUDIO_TOKEN" -H "Content-Type: application/json" -d "{\"model\":\"bge-m3\",\"input\":\"test\"}"
```

`200` 과 함께 숫자가 잔뜩 든 `embedding` 배열이 오면 정상입니다.
`404` 면 Studio XP 에 임베딩 경로가 안 열린 것 — LLM 제공사 문의 사항이며,
그동안은 단어 맞추기 검색으로 동작합니다.

### 4) 표 페이지는 AI 가 그림을 보고 옮겨 적습니다 (자동)

OCR 은 표의 **열을 조용히 빠뜨립니다** (실측: 9열 중 8열 누락). 그래서 적재할 때
표처럼 생긴 페이지는 이미지 AI(HCX-005)에 보내 Markdown 표로 옮기고, 그 숫자를
OCR 이 읽은 숫자와 대조합니다. 대조에 없는 숫자가 있으면 그 표는 **재확인 표시**가 붙고,
판정에서 그 인용만으로 "부적합" 을 내지 않습니다.

- 따로 켤 것 없습니다. `NDT_HCX_API_KEY` 가 있으면 1) 적재 때 같이 돕니다.
- 적재 로그 끝의 `table_stats` 를 보세요: `transcribed` 가 표 페이지 수만큼 나오면 정상,
  `vlm_failed` 가 있으면 ⑥ 연결 점검부터.
- 검색 결과에 `[재확인 conf=0.75]` 처럼 붙은 줄은 원문 표를 사람이 한 번 봐야 하는 것입니다.
- 끄고 싶으면 `config\hcx.yaml` 의 `table_pipeline: enabled: false`.

### 5) SCWEP 근거 — "도면에 없는 검사" 를 바로 과다청구로 보지 않습니다

SCWEP 는 **특수 공정의 검사 요구**를 담습니다. 예를 들어 "임시 러그를 떼면 그 자리에 PT" 같은 것인데,
이런 요구는 도면에 안 적힙니다. 그래서 도구는 이제 도면에 없는 검사가 청구되면
**제출된 SCWEP 를 먼저 보고**, 그 결과에 따라 네 가지로 갈라 적습니다 (검토 엑셀 `근거_상태` 칸).

- **근거 있음** — SCWEP 조항이 그 검사를 요구. 조건이 실제로 있었는지 한 번만 확인하면 됩니다.
- **근거 불명확** — 언급은 있는데 조건이 확실치 않음. 해당 조항을 직접 보세요.
- **근거 미제출** — SCWEP 가 없거나 적용 범위를 알 수 없음. **시공사에 제출을 요청**합니다.
- **근거 밖 — 과다청구** — 제출된 SCWEP 에도 없음. 이때만 부적합으로 확정합니다.

**시공사이 SCWEP 를 제출하면** PDF 를 `samples\scwep` 에 넣고 → 런처 ③ 표준·SCWEP 등록
(종류 확인 창에서 **SCWEP** 인지 보세요) → 그 회차를 **다시 검토**합니다. 이 순서를 지켜야 근거가 반영됩니다.

**이번 업데이트로 "과다 청구" 로 나오던 행 대부분이 "근거 미제출" 로 바뀝니다.** 도구가 고장난 게 아니라,
근거 서류를 받기 전에는 과다청구로 단정하지 않도록 바꾼 것입니다. 명백 불일치 건수가 크게 줄어드는 게 정상이고,
검토 끝에 찍히는 `basis_states` 요약에서 왜 줄었는지(미제출 N건 등)를 볼 수 있습니다.

**이미 검토·발송한 회차를 다시 돌리면** 판정과 위험도가 예전 엑셀과 달라집니다. 사실이 바뀐 게 아니라
입장을 바꾼 것이니, 기존 산출물과 그대로 대조하지 마세요.

## 4. 사내 AI 모델 비교 — 어느 모델이 우리 일에 맞나

사내에서 쓸 수 있는 AI는 **7종**입니다. 어느 것이 기성검증에 적합한지
실제 업무 케이스로 채점해 표로 보여줍니다.

| 모델 | 위치 | 성격 |
|---|---|---|
| `hcx007` | HCX | 추론 특화. 도구의 기본 모델 |
| `hcx005` | HCX | 이미지 입력 가능 |
| `gemma` | Studio XP | 컨텍스트 256K. 긴 문서 |
| `gptoss` | Studio XP | 대형 텍스트 |
| `hcxseed` | Studio XP | 한국어 |
| `hcxv2` | Studio XP | HCX 구버전 |
| `qwen` | Studio XP | 다국어 |

### 실행

**`run_model_ab.bat`** 더블클릭 — 도구 자체 점검만 합니다 (서버 접속 없음, 안전)

**7종 전부 비교하려면** 명령창(cmd)에서:

```
run_model_ab.bat "hcx007,hcx005,gemma,gptoss,hcxseed,hcxv2,qwen"
```

매번 치기 번거로우면 `run_model_ab.bat` 을 메모장으로 열어 맨 아래
`--providers %*` 를 `--providers hcx007,hcx005,gemma,gptoss,hcxseed,hcxv2,qwen`
로 바꿔 저장하면 더블클릭만으로 돌아갑니다.
(사내에 있는 파일을 고치는 것이라 결재와 무관합니다.)

호출량은 **7모델 × 12케이스 = 84회**입니다. HCX 일일 한도 10,000회 대비 미미합니다.

### 결과 보기

`data\outputs\ab_report.md` **← 이것만 보시면 됩니다**

모델별로 **권장 / 조건부 / 부적합** 판정이 표로 나옵니다.

### 채점 기준 — 왜 이렇게 보는가

| 항목 | 가중 | 무엇을 보는가 |
|---|---|---|
| **환각저항** | 3.0 | 근거가 없을 때 "근거 없음"이라 답하는가 |
| 근거인용 | 2.0 | 근거가 있을 때 실제로 찾아 인용하는가 |
| 매칭판정 | 2.0 | 청구행↔성적서. 무리한 매칭을 하지 않는가 |
| 적합성판정 | 1.5 | 부적합/정상을 옳게 가르는가 |
| 추출정확도 | 1.0 | OCR 오독 교정·문서번호 추출 |

**환각저항에서 하나라도 틀리면 점수와 무관하게 '부적합'** 입니다.
이 도구는 잘못된 판정이 곧 청구 금액 오류가 되므로, "지어내지 않는 것"을
정확도보다 무겁게 봅니다.

### 케이스를 우리 것으로 바꾸려면

`scripts\ab_cases.sample.json` (문제) 와 `scripts\ab_gold_ndt.json` (정답) 을
메모장으로 열어 실제 겪은 사례로 바꾸면 됩니다. 둘 다 결재 무관 파일이라
사외로 보내 새로 받을 필요도 없습니다.

## 5. 결과 파일 위치

| 종류 | 위치 |
|---|---|
| 검토 결과 엑셀 | `samples\billing\xxx_검토결과.xlsx` |
| 검사기준 가이드 | `data\outputs\검사기준_가이드_*.html` |
| 모델 비교 결과 | `data\outputs\ab_result.json` |
| 누적 검토 이력 | `data\ndt.sqlite` |
| 실행 로그 | `data\logs\날짜\*.log` |
| OCR 캐시 (영구) | `data\ocr_cache\*.json` |

---

## 6. 막혔을 때

1. `data\progress.log` 를 휴대폰으로 찍으세요. 한국어 진행 표시만 들어 있어 사진 한 장이면 충분합니다.
2. 검은 창의 마지막 30줄도 함께 찍으세요.
3. 또는 GUI **⑧ 버튼**으로 보고용 zip을 만드세요.

수정 파일을 받으면 대부분 **덮어쓰기만 하면 끝**입니다. `.py` `.yaml` `.md` `.whl` 은 결재가 필요 없습니다.

자주 나오는 증상별 대처는 `installer\사내_첫실행_문제해결.md` 에 정리되어 있습니다.

---

## 함께 들어있는 문서

| 문서 | 내용 |
|---|---|
| `사내IT_신청양식.md` | **방화벽·hosts 요청서 (복사해서 IT에 전달)** |
| `installer\사내_실행_가이드.md` | 기능별 상세 사용법 |
| `installer\사내_첫실행_문제해결.md` | 증상별 자가 해결 |
| `installer\README_설치.md` | 사외 빌드 담당자용 |
